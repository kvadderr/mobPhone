<?xml version="1.0" encoding="UTF-8"?>
<xct:Popup xmlns="http://xamarin.com/schemas/2014/forms" 
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
           Size="335, 220"
           BackgroundColor="Transparent"
           x:Class="MobPhone.Popups.PopupAchievementsConfirm">
    <Frame CornerRadius="12" Margin="0" Padding="0" Background="{StaticResource modalGradient}">
        <StackLayout>
            <Label Text="Подтверждаете отправку достижения?" Style="{StaticResource BoldText}" HorizontalTextAlignment="Center" FontSize="16" WidthRequest="200"/>

            <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                <Button Text="Нет" CornerRadius="12" />
                <Button Text="Да" CornerRadius="12" Background="{StaticResource greenGradient}" />
            </StackLayout>
        </StackLayout>
    </Frame>
</xct:Popup>
<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:custom="clr-namespace:MobPhone.Custom"
             x:Class="MobPhone.Views.ShareMenuSub">
  <ContentView.Content>
      <StackLayout Margin="20, 0, 20, 0" Spacing="20">
            <StackLayout Orientation="Horizontal">
                <Label x:Name="menuTitle" Text="Библиотека" VerticalOptions="Center" Style="{StaticResource BoldText}" FontSize="14" />
                <Image IsVisible="False" x:Name="img" Source="arrow_right" VerticalOptions="Center" />
                <Label IsVisible="False" x:Name="subMenuTitle" Text="Поддомен" VerticalOptions="Center" Style="{StaticResource BoldText}" FontSize="14" />
            </StackLayout>
            <ListView x:Name="MyListView"
                HasUnevenRows="True"
                BackgroundColor="Transparent"
                SeparatorVisibility="Default"
                VerticalScrollBarVisibility="Never"
                SelectionMode="None"
                ItemsSource="{Binding MyData}"
                ItemTemplate="{StaticResource shareSubMenu}" />
        </StackLayout>
  </ContentView.Content>
</ContentView>